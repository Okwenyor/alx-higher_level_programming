#!/usr/bin/python3

def print_matrix_integer(matrix=[[]]):
    for i in matrix:
        for v in range(len(i)):
            if v == len(i) - 1:
                print('{:d}' .format(i[v], end='')
                      else:
                      print('{:d}' .format(i[v]), end='')
                            print()
